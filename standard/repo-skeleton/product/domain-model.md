# Domain Model

The core entities, their relationships, and the rules that bind them. This vocabulary should match capability
names and `terminology.md`. Keep it conceptual - not a database schema.

## Entities

- **Entity** - definition, key attributes, lifecycle.

## Relationships

How entities relate (ownership, cardinality, invariants).

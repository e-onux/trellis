# Architecture Map

How capabilities, layers and external services relate. This is the reference `drift` checks compare against.

## Layers / boundaries

Describe the allowed dependency direction (e.g. UI → application → domain → infrastructure) and any forbidden
edges. The architecture invariants in `quality/architecture-invariants.md` make these machine-checkable.

## Capabilities ↔ services

| Capability | Owns | Talks to |
|---|---|---|
| … | … | … |

## Diagram

_Optional: link or embed a diagram. Keep it in sync or delete it (a stale diagram is worse than none)._

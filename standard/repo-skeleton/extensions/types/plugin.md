# Extension type: plugin / module

Self-contained feature units loaded by the host application. Beyond a provider, plugins often also touch routes,
menus/navigation, permissions/roles, event or hook registries, migrations/seeds, and CLI commands. List exactly
which of these your host requires in the point-specific `EXTENSIONS.md` and `extension-registry.yaml`.
